#!/bin/sh

set -e

if [ "$1" = "remove" ] || [ "$1" = "disappear" ]; then
	update-alternatives --remove gunicorn_pecan /usr/bin/gunicorn_pecan-python3
	update-alternatives --remove pecan /usr/bin/pecan-python3
fi

#DEBHELPER#

exit 0
